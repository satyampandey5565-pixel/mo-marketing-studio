const menuToggle=document.querySelector('.menu-toggle'),mobileMenu=document.querySelector('#mobile-menu');function closeMenu(){if(!mobileMenu)return;mobileMenu.hidden=true;document.body.classList.remove('menu-open');menuToggle?.setAttribute('aria-expanded','false')}menuToggle?.addEventListener('click',()=>{const open=menuToggle.getAttribute('aria-expanded')==='true';menuToggle.setAttribute('aria-expanded',String(!open));mobileMenu.hidden=open;document.body.classList.toggle('menu-open',!open)});mobileMenu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));const ro=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');ro.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -40px'});document.querySelectorAll('.reveal').forEach(e=>ro.observe(e));const cursor=document.querySelector('.cursor');if(cursor&&matchMedia('(pointer:fine)').matches){let x=innerWidth/2,y=innerHeight/2,tx=x,ty=y;addEventListener('mousemove',e=>{tx=e.clientX;ty=e.clientY});(function tick(){x+=(tx-x)*.18;y+=(ty-y)*.18;cursor.style.left=x+'px';cursor.style.top=y+'px';requestAnimationFrame(tick)})();document.querySelectorAll('a,button').forEach(e=>{e.addEventListener('mouseenter',()=>cursor.classList.add('hover'));e.addEventListener('mouseleave',()=>cursor.classList.remove('hover'))})}const lb=document.querySelector('#lightbox'),cap=document.querySelector('.lightbox-caption');document.querySelectorAll('[data-lightbox]').forEach(e=>e.addEventListener('click',()=>{if(!lb)return;cap.textContent=e.dataset.lightbox;lb.hidden=false;document.body.classList.add('menu-open');lb.querySelector('.lightbox-close').focus()}));function closeLb(){if(!lb)return;lb.hidden=true;document.body.classList.remove('menu-open')}lb?.querySelector('.lightbox-close')?.addEventListener('click',closeLb);lb?.addEventListener('click',e=>{if(e.target===lb)closeLb()});document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeMenu();if(lb&&!lb.hidden)closeLb()}});const form=document.querySelector('#contact-form'),status=document.querySelector('.form-status');form?.addEventListener('submit',e=>{e.preventDefault();if(!form.checkValidity()){status.textContent='Please complete the required fields before sending.';form.reportValidity();return}const name=new FormData(form).get('name');status.textContent=`Thanks, ${name}. Your enquiry is ready. Connect this form to Formspree or your preferred backend to receive submissions.`;form.reset()});
